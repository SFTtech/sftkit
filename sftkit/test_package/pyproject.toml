[build-system]
requires = ["uv_build>=0.9.9,<0.10.0"]
build-backend = "uv_build"

[project]
name = "test_package"
version = "0.0.1"
requires-python = ">=3.11"

[tool.sftkit]
db_code_dir = "test_package/db_code"
db_migrations_dir = "test_package/db_migrations"
